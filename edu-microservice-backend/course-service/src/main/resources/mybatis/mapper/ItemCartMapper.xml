<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app.command.api.data.ItemCartMapper">
	<resultMap id="BaseResultMap" type="app.command.api.data.ItemCartPo">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="quantity" jdbcType="INT" property="quantity" />
		<result column="variant_id" jdbcType="BIGINT" property="variantId" />
		<result column="user_id" jdbcType="BIGINT" property="userId" />
	</resultMap>
	<sql id="Base_Column_List">
		idquantity, variant_id, user_id, 
	</sql>
	<select id="findAll" resultMap="BaseResultMap">
		select <include refid="Base_Column_List" /> from item_cart
	</select>
	<select id="getById" resultMap="BaseResultMap">
		select <include refid="Base_Column_List" /> from item_cart where id = #{id, jdbcType=BIGINT}
	</select>
	<insert id="save" parameterType="app.command.api.data.ItemCartPo">
		insert into item_cart (<include refid="Base_Column_List" />, <include refid="Blob_Column_List" />) 
		values (#{quantity, jdbcType=INT}, #{variantId, jdbcType=BIGINT}, #{userId, jdbcType=BIGINT}, #{id, jdbcType=BIGINT})
	</insert>
	<update id="updateById" parameterType="app.command.api.data.ItemCartPo">
		update item_cart
			<set>
				<if test="quantity != null">
					quantity = #{quantity, jdbcType=INT}, 
				</if>
				<if test="variantId != null">
					variant_id = #{variantId, jdbcType=BIGINT}, 
				</if>
				<if test="userId != null">
					user_id = #{userId, jdbcType=BIGINT}, 
				</if>
				<if test="id != null">
					id = #{id, jdbcType=BIGINT}, 
				</if>

			</set> 
		where id = #{id, jdbcType=BIGINT}
	</update>
	<delete id="deleteById">
		delete from item_cart where id = #{id, jdbcType=BIGINT}
	</delete>
	<select id="pagination" parameterType="app.command.api.data.Pagination" resultMap="BaseResultMap">
		select <include refid="Base_Column_List" /> from item_cart 
		order by id desc 
		limit #{offset, jdbcType=BIGINT} ,#{limit,jdbcType=INTEGER}
	</select>
	<select id="countAll" resultType="java.lang.Long">
		select count(*) from item_cart
	</select>
</mapper>